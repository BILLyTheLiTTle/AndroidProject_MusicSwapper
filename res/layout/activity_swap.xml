<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".SwapActivity" >

    <TextView
        android:id="@+id/prompt_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="14dp"
        android:layout_marginTop="23dp"
        android:textStyle="bold"
        android:text="@string/prompt" />

    <Spinner
        android:id="@+id/music_type_spinner"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/prompt_textView"
        android:layout_below="@+id/prompt_textView"
        android:entries="@array/music_types"
        android:layout_marginTop="10dp" />

    <Button
        android:id="@+id/swap_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/music_type_spinner"
        android:layout_below="@+id/music_type_spinner"
        android:text="@string/swap" 
        android:onClick="swapSongs"/>

</RelativeLayout>